import { HostlistinerDirective } from './hostlistiner.directive';

describe('HostlistinerDirective', () => {
  it('should create an instance', () => {
    const directive = new HostlistinerDirective();
    expect(directive).toBeTruthy();
  });
});
